IFL.MapVis.MarkerOperator = function(_domain, _visObjects) {
    var domain = _domain;
    var icons = _visObjects || [];
    
    var eventHandler = new IFL.Util.EventHandler(['mouseover', 'mouseout', 'click']);
    return {
    }
}
